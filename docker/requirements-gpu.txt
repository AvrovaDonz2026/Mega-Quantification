# Installed *after* the CUDA PyTorch wheel. Do not pin torch here.
# The 5090 production PTQ used this exact release. 0.48 is not on PyPI.
nvidia-modelopt[hf]==0.46.1
llmcompressor>=0.8.0
compressed-tensors>=0.10.0
transformers>=4.56.0
datasets>=3.0.0
accelerate>=1.0.0
safetensors>=0.5.0
sentencepiece>=0.2.0
protobuf>=5.0.0
einops>=0.8.0
huggingface_hub>=0.26.0
pydantic>=2.6.0
pyyaml>=6.0.0
tqdm>=4.66.0
# Qwen3.8 Gated DeltaNet fused kernels (Triton / Hub). causal-conv1d is a
# CUDA extension — install separately with TORCH_CUDA_ARCH_LIST=12.0 on 5090.
kernels>=0.9
flash-linear-attention>=0.3
